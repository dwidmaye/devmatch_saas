.row
  .col-md-6.col-md-offset-3
    %h1.text-center= "#{params[:plan] == '1' ? 'Basic' : 'Pro'} Account"
  .col-md-6.col-md-offset-3
    .well
      %h2 Sign up
      = render "basic_form"
      = render "devise/shared/links"
